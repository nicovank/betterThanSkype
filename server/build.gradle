plugins {
    id 'application'
}

application {
    mainClassName = "Main"
}

dependencies {
    compile project(":api")
}

jar {
    manifest{
        attributes 'Main-Class': 'Main'
    }

    from configurations.compile.collect { zipTree(it) }
}